import { OldUserLoginDirective } from './old-user-login.directive';

describe('OldUserLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new OldUserLoginDirective();
    expect(directive).toBeTruthy();
  });
});

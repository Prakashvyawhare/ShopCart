import { UserValidationDirective } from './user-validation.directive';

describe('UserValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new UserValidationDirective();
    expect(directive).toBeTruthy();
  });
});

import { LoginCaptchaDirective } from './login-captcha.directive';

describe('LoginCaptchaDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginCaptchaDirective();
    expect(directive).toBeTruthy();
  });
});

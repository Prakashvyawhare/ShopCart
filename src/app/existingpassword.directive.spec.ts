import { ExistingpasswordDirective } from './existingpassword.directive';

describe('ExistingpasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new ExistingpasswordDirective();
    expect(directive).toBeTruthy();
  });
});

import { DoubliPasswordDirective } from './doubli-password.directive';

describe('DoubliPasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new DoubliPasswordDirective();
    expect(directive).toBeTruthy();
  });
});
